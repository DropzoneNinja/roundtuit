# Backend
DATABASE_URL=postgresql://roundtuit:password@localhost:5432/roundtuit
JWT_SECRET=change-me-to-a-random-32-byte-hex-string
JWT_EXPIRES_IN=7d
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:80

# Frontend
VITE_API_URL=

# Backups — set to an external host path where backup files should be stored
# e.g. /mnt/nas/roundtuit-backups or /home/user/backups
BACKUP_DIR=./backups
