# API base URL — leave empty when the frontend is served behind nginx at the same origin.
# Set to the full backend URL only if running the frontend standalone (e.g. in dev without nginx).
VITE_API_URL=
